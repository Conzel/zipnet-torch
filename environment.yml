name: zipnet-torch
channels:
  - conda-forge
dependencies:
  - python=3.9
  - tensorboard=2.8.*
  - pip:
    - constriction==0.2.*
    - compressai==1.1.*
    - setuptools==59.5.0
    - torch=1.10.*
    - torchvision=0.11.*
prefix: /opt/homebrew/Caskroom/mambaforge/base/envs/zipnet-torch
